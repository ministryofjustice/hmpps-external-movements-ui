{% extends "partials/layout.njk" %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% set pageTitle = 'Select absence type - Manage temporary absences' %}

{% block innerContent %}
  <div class="govuk-width-container">
    <span class="govuk-caption-l">Manage Temporary Absences</span>
    <form method="post">
      <input type="hidden" name="_csrf" value="{{ csrfToken }}" />
      {{ govukRadios({
        name: "absenceType",
        errorMessage: validationErrors | findError('absenceType'),
        fieldset: {
          legend: {
            text: "What type of absence is this?",
            isPageHeading: true,
            classes: "govuk-fieldset__legend--xl"
          }
        },
        items: options | setCheckedValue(absenceType)
      }) }}

      {{ govukButton({
        text: "Continue",
        preventDoubleClick: true
      }) }}
    </form>
  </div>
{% endblock %}

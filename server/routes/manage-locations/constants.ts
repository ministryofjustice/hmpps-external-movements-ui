export const INVALID_LOCATIONS_VERSION_MSG = JSON.stringify({
  apiError: [
    'Another user has made changes to the temporary absence locations. Please review the new list and try your action again if required.',
  ],
})
